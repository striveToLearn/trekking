import { LazySectionDirective } from './lazy-section.directive';

describe('LazySectionDirective', () => {
  it('should create an instance', () => {
    const directive = new LazySectionDirective();
    expect(directive).toBeTruthy();
  });
});
